<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <!-- Para que aparezca la barra de estado debe estar quitado NoActionBar -->
    <!--<style name="Base.Theme.AppJAR" parent="Theme.Material3.DayNight.NoActionBar">-->
    <style name="Base.Theme.AppJAR" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/miazul</item>
        <!--<item name="colorSurface">@color/miactionbarcolor</item>-->
    </style>

    <style name="Theme.AppJAR" parent="Base.Theme.AppJAR" />
    <style name="Theme.AppJAR.SinBarra" parent="Theme.Material3.DayNight.NoActionBar" />

    <!-- exclusivamente para hacer las transiciones de animaciones -->
    <style name="Theme.Transiciones" parent="Base.Theme.AppJAR" >
        <item name="android:windowContentTransitions">true</item>
<!--        <item name="android:windowSharedElementEnterTransition">@transition/change_image_transform</item>-->
<!--        <item name="android:windowSharedElementExitTransition">@transition/change_image_transform</item>-->
        <item name="android:windowEnterTransition">@transition/slide</item>
        <item name="android:windowExitTransition">@transition/slide</item>
        <!--<item name="android:windowEnterTransition">@transition/explode</item>
        <item name="android:windowExitTransition">@transition/explode</item>-->
        <!--  Estados Reingreso-Retorno   -->
        <item name="android:windowReenterTransition">@transition/fade</item>
        <item name="android:windowReturnTransition">@transition/fade</item>
        <!--  para que no empiece la de entrada hasta que acabe la de salida y vice  -->
        <item name="android:windowAllowEnterTransitionOverlap">false</item>
        <item name="android:windowAllowReturnTransitionOverlap">false</item>
        <!--  Habilitar superposición de los elementos compartidos entre transiciones  -->
        <item name="android:windowSharedElementsUseOverlay">true</item>
    </style>
</resources>